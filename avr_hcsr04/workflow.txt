Timer1 ISR fires every 60ms
    ↓
trigger_flag = 1
    ↓
main() detects trigger_flag == 1
    ↓
trigger_flag = 0  (reset immediately)
    ↓
hcsr04_trigger()  (send pulse)
    ↓
INT0 ISR measures echo
    ↓
get_distance_cm() calculates result
    ↓
wait for next Timer1 fire